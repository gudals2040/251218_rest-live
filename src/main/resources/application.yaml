server:
  port: 8080

spring:
  application:
    name: 251218_restapi

  profiles:
    active: dev

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB